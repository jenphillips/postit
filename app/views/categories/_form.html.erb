<%= render "shared/errors", entity: @category %>


<%= form_for @category do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>
	<br />

	<%= f.submit "Create/Update Category", class: "btn btn-primary" %>

<% end %>